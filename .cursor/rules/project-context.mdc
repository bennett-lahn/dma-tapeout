---
description: Load TinyDMA project context and planning constraints
alwaysApply: true
---

# TinyDMA Project Context

This repo is **TinyDMA** for Tiny Tapeout in **Phase 2** (V1 feature RTL under `src/`; cocotb M0–M5 accepted; manual IHP LibreLane harden closed 1x1 @ 66 MHz), targeting **IHP SG13G2 / TTIHP26b** (D27).

## Before nontrivial work

Read `docs/llm/00-index.md` and the files it lists for the task at hand. Prefer those docs over chat memory. For local ASIC harden / LibreLane / area: `docs/llm/13-hardening-librelane.md` (human: `docs/human/architecture/hardening.md`).

When discussing PSRAM timing parameters, testbench timing parameters, or verification IDs (`Q-*`, `FP-*`, etc.), define each term inline on first use; see `.cursor/rules/verification-vocabulary.mdc`.

Human summaries live in `docs/human/` (condensed, not stubs). Every durable fact in `docs/llm/` must appear in `docs/human/` in some form; llm elaborates, it does not hold private truth. Do not grow human docs into a second full dump, and do not leave new requirements llm-only behind a pointer (anti-pattern: `CHK-*` only in `docs/llm/verification/06-checkers.md`).

## Hard constraints

- Budget: **1x1 only** (one Tiny Tapeout tile; D36). `1x2` / 2-tile is out of budget. Soft caution ~200 DFFs on 1x1 (first harden: ~158 DFFs, ~60% util). Hard gate: fits and times on `tt_block_1x1`. If N=5 does not close, cut area; never grow tiles.
- PDK / shuttle: **ihp-sg13g2** / **TTIHP26b** (not sky130); do not cite sky130 GPIO MHz ratings as binding
- Start from scratch; TinyDMA-2C is prior art only, not a code source
- Obsidian notes under `C:\Users\lahnb\Documents\Obsidian Vault\Projects\Tiny Tapeout\` are **read-only**
- Prefer shipping the verified V1 bulk mover; do not grow RTL beyond the frozen feature set
- When proposing features, state DFF / tile impact and treat RTL growth as out of scope unless the user reopens it
- **Memory scope (D11/D12/D19):** both PSRAM devices are DMA endpoints (incl. cross-device); shipped RTL is bulk memcpy via 11-byte TCDs (`QUIT` end-of-chain; no ALU/ring/cond-stop); ASIC does not master flash (MCU pass-through only)

## TinyDMA-2C prior art (separate context)

- Canonical dump: `docs/llm/prior-art/tinydma-2c.md`
- Skill: `.cursor/skills/tinydma-prior-art/SKILL.md`
- **If any claim is drawn from that context, say so explicitly** (e.g. "Per TinyDMA-2C prior art...").
- Do not present TinyDMA-2C pins, protocols, or widths as this project's decisions unless the user adopts them and docs are updated.

## Implementation style (when RTL exists)

- SystemVerilog port/instance commas at the beginning of lines
- Synchronous active-low reset only (`rst_n`; assert with `if (~rst_n)`)
- No unsolicited extra markdown docs outside `docs/`
- GitHub `tt-gds-action` port check: `info.yaml` `source_files` lists `top.v` first; that wrapper must not `import` SV packages (yowasp Yosys). Sim Makefile still compiles `types.svh` first.
