# Reusable demoboard layer: pins (signal map), board (DemoBoard access), qspi
# (PIO SPI / QPI transports). Import submodules explicitly; this marker stays
# empty so MicroPython does not pull rp2 in when only `pins` is needed.
